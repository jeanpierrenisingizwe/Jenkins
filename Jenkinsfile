 pipeline { 
    agent any  // Runs on any available agent 
    stage('Build') {
    steps {
        bat 'echo Building the project...'
        bat 'dir'  // Example: Windows directory listing
    }
}

        
            } 
        } 
        stage('Test') { 
            steps { 
                echo "Running tests..." 
            } 
        } 
        stage('Deploy') { 
            steps { 
                echo "Deploying..." 
            } 
        } 
    } 
} 
